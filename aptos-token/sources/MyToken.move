module web3lab::MyToken {
    use aptos_token::token;
    use std::signer;
    use aptos_framework::account;
    use aptos_framework::coin;
    use aptos_framework::aptos_coin;

    entry public fun boot(){
        let x = 128u128;
    }
}
